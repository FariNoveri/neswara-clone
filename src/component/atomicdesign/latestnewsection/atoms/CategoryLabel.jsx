import React from "react";

const CategoryLabel = ({ category }) => {
  return <span className="text-sm font-semibold text-black uppercase mb-2 block">{category}</span>;
};

export default CategoryLabel;
